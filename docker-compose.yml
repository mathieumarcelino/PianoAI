version: '3.8'

networks:
  proxy:
    external: true

services:
  piano-app:
    container_name: piano-app
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - proxy
